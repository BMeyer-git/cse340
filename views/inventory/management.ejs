<%# Content heading %>
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<%# Message block %>
<%- messages() %>

<%# Error block %>
<% if (errors) { %>
    <ul class="notice">
   <% errors.array().forEach(error => { %>
     <li><%= error.msg %></li>
  <%  }) %>
   </ul>
  <% } %>

<%# Inventory management display %>
<a href="../../inv/add-classification/">Add New Classification</a><br>
<a href="../../inv/add-inventory/">Add New Vehicle</a>
<h2>Manage Inventory</h2>
<p>Select a classification from the list to see vehicles belonging to that classification</p>
<%- classificationSelect %>
<table id="inventoryDisplay"></table>
<noscript>JavaScript must be enabled to use this page.</noscript>
<script src="../../js/inventory.js"></script>